// preloaderS
const preLoad = document.querySelector(".preLoader");
window.addEventListener("load",function() {
    preLoad.style.display = "none";
})

// on scrolling top bar disapper 
const topBar = document.querySelector('.topBar');
